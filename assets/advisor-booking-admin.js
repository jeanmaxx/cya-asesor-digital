import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

const cfg=window.SUPABASE_CONFIG||{};
if(!cfg.url||!cfg.publishableKey) throw new Error('TTD agenda admin: Supabase no configurado.');
const supabase=createClient(cfg.url,cfg.publishableKey);
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const days=[['1','Lunes'],['2','Martes'],['3','Miércoles'],['4','Jueves'],['5','Viernes'],['6','Sábado'],['0','Domingo']];
let currentAdvisorId=null,currentSlug='',removedLocations=new Set(),serviceMap=new Map(),locationMap=new Map();

function injectUI(){
  if($('ttd-booking-admin-fieldset'))return;
  const services=$('services-editor')?.closest('fieldset');if(!services)return;
  const loc=document.createElement('fieldset');loc.id='ttd-locations-admin-fieldset';loc.innerHTML=`<legend>Puntos de atención</legend><p class="hint">Puedes publicar varios puntos aunque tú no estés físicamente en todos. Usa “Atención con cita” solo donde realmente puedan recibir al cliente.</p><div id="ttd-locations-admin"></div><div class="ttd-admin-actions"><button id="ttd-add-location" class="secondary-btn" type="button">+ Agregar punto de atención</button></div>`;
  const booking=document.createElement('fieldset');booking.id='ttd-booking-admin-fieldset';booking.innerHTML=`<legend>Agenda TTD · piloto</legend><p class="hint">La cita queda registrada en TTD. En esta primera versión la confirmación es manual y Google Calendar funciona como apoyo, no como fuente principal.</p><div class="ttd-checks"><label><input id="ttd-booking-enabled" type="checkbox"> Activar agenda pública</label><label><input id="ttd-allow-in-person" type="checkbox"> Presencial</label><label><input id="ttd-allow-phone" type="checkbox"> Telefónica</label><label><input id="ttd-allow-video" type="checkbox"> Videollamada</label></div><div class="ttd-booking-settings-grid"><label>Duración estándar (min)<input id="ttd-duration" type="number" min="15" max="240" step="15" value="45"></label><label>Intervalo entre horarios (min)<select id="ttd-slot"><option value="15">15</option><option value="30" selected>30</option><option value="45">45</option><option value="60">60</option></select></label><label>Aviso mínimo (horas)<input id="ttd-notice" type="number" min="0" max="168" value="4"></label><label>Mostrar agenda hasta (días)<input id="ttd-horizon" type="number" min="1" max="365" value="60"></label><label>Confirmación<select id="ttd-confirmation"><option value="manual">Manual</option><option value="auto">Automática</option></select></label></div><h3 style="margin:18px 0 7px">Disponibilidad semanal</h3><p class="ttd-admin-note">Por ahora se usa un bloque de horario por día. Más adelante podremos añadir descansos y horarios distintos por ubicación.</p><div id="ttd-week-grid" class="ttd-week-grid"></div><div class="ttd-admin-actions"><button id="ttd-save-booking" class="primary-btn" type="button">Guardar agenda y ubicaciones</button><button id="ttd-refresh-appointments" class="secondary-btn" type="button">Actualizar citas</button><span id="ttd-booking-admin-msg" class="message"></span></div><h3 style="margin:22px 0 7px">Solicitudes recientes</h3><div id="ttd-appointments" class="ttd-appointment-list"><p class="ttd-empty">Sin solicitudes todavía.</p></div>`;
  services.after(loc,booking);
  renderWeekRows();
  $('ttd-add-location').onclick=()=>addLocationRow({});
  $('ttd-save-booking').onclick=saveAll;
  $('ttd-refresh-appointments').onclick=()=>loadAppointments();
}

function renderWeekRows(){const root=$('ttd-week-grid');if(!root)return;root.replaceChildren();days.forEach(([key,name])=>{const row=document.createElement('div');row.className='ttd-week-row';row.dataset.weekday=key;row.innerHTML=`<label><input class="ttd-day-enabled" type="checkbox"> ${name}</label><input class="ttd-day-start" type="time" value="10:00"><input class="ttd-day-end" type="time" value="17:00">`;root.appendChild(row)})}
function addLocationRow(data){
  const root=$('ttd-locations-admin');if(!root)return;const card=document.createElement('div');card.className='ttd-admin-card';if(data.id)card.dataset.id=data.id;
  card.innerHTML=`<div class="ttd-admin-card-head"><strong>${esc(data.name||'Nuevo punto de atención')}</strong><button class="ttd-remove-location ttd-danger-btn" type="button">Eliminar</button></div><div class="ttd-location-admin-grid"><label>Nombre<input class="ttd-loc-name" maxlength="100" value="${esc(data.name||'')}" placeholder="Ej. Tequisquiapan Centro"></label><label>Ciudad<input class="ttd-loc-city" maxlength="100" value="${esc(data.city||'')}"></label><label>Estado<input class="ttd-loc-state" maxlength="100" value="${esc(data.state||'')}"></label><label class="ttd-wide">Dirección o referencia<input class="ttd-loc-address" maxlength="240" value="${esc(data.address||'')}"></label><label class="ttd-wide">Enlace de Google Maps<input class="ttd-loc-maps" type="url" value="${esc(data.maps_url||'')}" placeholder="https://maps.google.com/..."></label><label class="ttd-wide">Indicaciones / modalidad<textarea class="ttd-loc-instructions" rows="2" maxlength="300">${esc(data.instructions||'')}</textarea></label><label><span>Visible</span><input class="ttd-loc-active" type="checkbox" ${data.is_active!==false?'checked':''}></label><label><span>Permitir citas presenciales</span><input class="ttd-loc-appointment" type="checkbox" ${data.appointment_enabled!==false?'checked':''}></label></div>`;
  card.querySelector('.ttd-loc-name').addEventListener('input',e=>{card.querySelector('.ttd-admin-card-head strong').textContent=e.target.value||'Nuevo punto de atención'});
  card.querySelector('.ttd-remove-location').onclick=()=>{if(card.dataset.id)removedLocations.add(card.dataset.id);card.remove()};root.appendChild(card);
}
function showMsg(text,error=false){const el=$('ttd-booking-admin-msg');if(!el)return;el.textContent=text;el.classList.toggle('ttd-save-error',error);el.classList.toggle('ttd-save-ok',!error&&!!text)}

async function loadForCurrentEditor(force=false){
  injectUI();const panel=$('editor-panel');if(!panel||panel.classList.contains('is-hidden'))return;const slug=$('slug')?.value?.trim();if(!slug)return;if(!force&&slug===currentSlug&&currentAdvisorId)return;currentSlug=slug;currentAdvisorId=null;removedLocations=new Set();showMsg('Cargando agenda…');
  const{data:profile,error}=await supabase.from('advisor_profiles').select('id').eq('slug',slug).maybeSingle();if(error||!profile){showMsg('Guarda primero la tarjeta para configurar agenda y ubicaciones.',true);return}currentAdvisorId=profile.id;
  const[{data:settings},{data:locations},{data:availability},{data:appointments},{data:services}]=await Promise.all([
    supabase.from('advisor_booking_settings').select('*').eq('advisor_id',profile.id).maybeSingle(),
    supabase.from('advisor_locations').select('*').eq('advisor_id',profile.id).order('sort_order'),
    supabase.from('advisor_availability').select('*').eq('advisor_id',profile.id).eq('is_active',true),
    supabase.from('advisor_appointments').select('*').eq('advisor_id',profile.id).order('starts_at',{ascending:false}).limit(30),
    supabase.from('advisor_services').select('id,title').eq('advisor_id',profile.id)
  ]);
  serviceMap=new Map((services||[]).map(x=>[x.id,x.title]));locationMap=new Map((locations||[]).map(x=>[x.id,x.name]));
  applySettings(settings||{});renderLocationsAdmin(locations||[]);applyAvailability(availability||[]);renderAppointments(appointments||[]);showMsg('');
}
function applySettings(s){$('ttd-booking-enabled').checked=!!s.booking_enabled;$('ttd-allow-in-person').checked=s.allow_in_person!==false;$('ttd-allow-phone').checked=s.allow_phone!==false;$('ttd-allow-video').checked=s.allow_video!==false;$('ttd-duration').value=s.default_duration_minutes||45;$('ttd-slot').value=String(s.slot_minutes||30);$('ttd-notice').value=s.min_notice_hours??4;$('ttd-horizon').value=s.max_days_ahead||60;$('ttd-confirmation').value=s.confirmation_mode||'manual'}
function renderLocationsAdmin(rows){const root=$('ttd-locations-admin');root.replaceChildren();rows.forEach(addLocationRow);if(!rows.length)root.innerHTML='<p class="ttd-empty">Aún no hay puntos de atención. Puedes añadir uno o dejar solo atención telefónica/videollamada.</p>'}
function applyAvailability(rows){document.querySelectorAll('.ttd-week-row').forEach(row=>{const list=rows.filter(x=>String(x.weekday)===row.dataset.weekday);const item=list[0];row.querySelector('.ttd-day-enabled').checked=!!item;if(item){row.querySelector('.ttd-day-start').value=String(item.start_time).slice(0,5);row.querySelector('.ttd-day-end').value=String(item.end_time).slice(0,5)}})}
function fmtDate(v){try{return new Intl.DateTimeFormat('es-MX',{dateStyle:'medium',timeStyle:'short',timeZone:'America/Mexico_City'}).format(new Date(v))}catch{return v||''}}
function renderAppointments(rows){const root=$('ttd-appointments');root.replaceChildren();if(!rows.length){root.innerHTML='<p class="ttd-empty">Sin solicitudes todavía.</p>';return}rows.forEach(a=>{const card=document.createElement('article');card.className='ttd-appointment';const mode={in_person:'Presencial',phone:'Telefónica',video:'Videollamada'}[a.mode]||a.mode;card.innerHTML=`<div class="ttd-appointment-top"><div><h4>${esc(a.customer_name)}</h4><p>${esc(fmtDate(a.starts_at))} · ${esc(mode)}${a.service_id?` · ${esc(serviceMap.get(a.service_id)||'Servicio')}`:''}${a.location_id?` · ${esc(locationMap.get(a.location_id)||'Punto de atención')}`:''}</p><p>${esc(a.customer_phone)}${a.customer_email?` · ${esc(a.customer_email)}`:''}</p>${a.customer_notes?`<p>${esc(a.customer_notes)}</p>`:''}</div><span class="ttd-appointment-status">${esc(a.status)}</span></div><div class="ttd-appointment-buttons"></div>`;const buttons=card.querySelector('.ttd-appointment-buttons');[['confirmed','Confirmar'],['rejected','Rechazar'],['completed','Completada'],['cancelled','Cancelar'],['no_show','No asistió']].forEach(([status,label])=>{if(a.status===status)return;const b=document.createElement('button');b.type='button';b.textContent=label;b.onclick=()=>updateAppointment(a.id,status);buttons.appendChild(b)});root.appendChild(card)})}
async function loadAppointments(){if(!currentAdvisorId)return;const{data,error}=await supabase.from('advisor_appointments').select('*').eq('advisor_id',currentAdvisorId).order('starts_at',{ascending:false}).limit(30);if(error){showMsg(error.message,true);return}renderAppointments(data||[])}
async function updateAppointment(id,status){const{error}=await supabase.from('advisor_appointments').update({status}).eq('id',id);if(error){showMsg(error.message,true);return}showMsg('Estado de cita actualizado.');await loadAppointments()}

async function saveAll(){
  if(!currentAdvisorId){showMsg('Guarda primero la tarjeta principal.',true);return}const btn=$('ttd-save-booking');btn.disabled=true;showMsg('Guardando…');
  try{
    const settings={advisor_id:currentAdvisorId,booking_enabled:$('ttd-booking-enabled').checked,default_duration_minutes:Number($('ttd-duration').value)||45,slot_minutes:Number($('ttd-slot').value)||30,min_notice_hours:Number($('ttd-notice').value)||0,max_days_ahead:Number($('ttd-horizon').value)||60,allow_in_person:$('ttd-allow-in-person').checked,allow_phone:$('ttd-allow-phone').checked,allow_video:$('ttd-allow-video').checked,confirmation_mode:$('ttd-confirmation').value||'manual'};
    let r=await supabase.from('advisor_booking_settings').upsert(settings,{onConflict:'advisor_id'});if(r.error)throw r.error;
    if(removedLocations.size){r=await supabase.from('advisor_locations').delete().in('id',[...removedLocations]);if(r.error)throw r.error}
    const cards=[...document.querySelectorAll('#ttd-locations-admin .ttd-admin-card')];let sort=0;for(const card of cards){const name=card.querySelector('.ttd-loc-name').value.trim();if(!name)continue;const row={advisor_id:currentAdvisorId,name,city:card.querySelector('.ttd-loc-city').value.trim()||null,state:card.querySelector('.ttd-loc-state').value.trim()||null,address:card.querySelector('.ttd-loc-address').value.trim()||null,maps_url:card.querySelector('.ttd-loc-maps').value.trim()||null,instructions:card.querySelector('.ttd-loc-instructions').value.trim()||null,is_active:card.querySelector('.ttd-loc-active').checked,appointment_enabled:card.querySelector('.ttd-loc-appointment').checked,sort_order:sort++};if(card.dataset.id)row.id=card.dataset.id;r=await supabase.from('advisor_locations').upsert(row);if(r.error)throw r.error}
    r=await supabase.from('advisor_availability').delete().eq('advisor_id',currentAdvisorId);if(r.error)throw r.error;const availability=[...document.querySelectorAll('.ttd-week-row')].filter(row=>row.querySelector('.ttd-day-enabled').checked).map(row=>({advisor_id:currentAdvisorId,weekday:Number(row.dataset.weekday),start_time:row.querySelector('.ttd-day-start').value,end_time:row.querySelector('.ttd-day-end').value,is_active:true})).filter(x=>x.start_time&&x.end_time&&x.end_time>x.start_time);if(availability.length){r=await supabase.from('advisor_availability').insert(availability);if(r.error)throw r.error}
    removedLocations=new Set();showMsg('Agenda y ubicaciones guardadas correctamente.');await loadForCurrentEditor(true);
  }catch(err){showMsg(err.message||'No fue posible guardar la agenda.',true)}finally{btn.disabled=false}
}

injectUI();
const observer=new MutationObserver(()=>{if(!$('editor-panel')?.classList.contains('is-hidden'))setTimeout(()=>loadForCurrentEditor(),80)});if($('editor-panel'))observer.observe($('editor-panel'),{attributes:true,attributeFilter:['class']});
$('slug')?.addEventListener('change',()=>loadForCurrentEditor(true));
setInterval(()=>{if(!$('editor-panel')?.classList.contains('is-hidden')){const slug=$('slug')?.value?.trim();if(slug&&slug!==currentSlug)loadForCurrentEditor(true)}},900);
