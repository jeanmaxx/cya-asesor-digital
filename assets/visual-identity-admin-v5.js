import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cfg=window.SUPABASE_CONFIG||{},supabase=createClient(cfg.url,cfg.publishableKey),$=id=>document.getElementById(id),storageUrl=p=>p?`${cfg.url}/storage/v1/object/public/${cfg.storageBucket}/${p}`:'';
const state={kind:'',table:'',form:null,id:'',profile:null,pending:{main:null,icon:null,dark:null,photo:null},urls:new Map(),lastSlug:'',saving:null};
const defaults={preset:'personal',show_logo:true,show_photo:true,logo_size:'medium',photo_size:'large',logo_shape:'none',photo_shape:'circle',logo_fit:'contain',photo_fit:'cover',logo_position:'stacked',logo_asset:'main',header_text_size:'medium',header_text_align:'left'};
const presets={personal:{show_logo:true,show_photo:true,logo_size:'medium',photo_size:'large',logo_shape:'none',photo_shape:'circle',logo_fit:'contain',photo_fit:'cover',logo_position:'stacked'},corporate:{show_logo:true,show_photo:false,logo_size:'large',photo_size:'medium',logo_shape:'none',photo_shape:'circle',logo_fit:'contain',photo_fit:'cover',logo_position:'distributed'},mixed:{show_logo:true,show_photo:true,logo_size:'medium',photo_size:'medium',logo_shape:'none',photo_shape:'circle',logo_fit:'contain',photo_fit:'cover',logo_position:'grouped_left'},minimal:{show_logo:false,show_photo:false,logo_size:'small',photo_size:'small',logo_shape:'none',photo_shape:'circle',logo_fit:'contain',photo_fit:'cover',logo_position:'stacked'}};
function detect(){if($('profile-form')){state.kind='advisor';state.table='advisor_profiles';state.form=$('profile-form');return true}if($('business-form')){state.kind='business';state.table='business_profiles';state.form=$('business-form');state.id=new URLSearchParams(location.search).get('id')||'';return true}if($('other-form')){state.kind='other';state.table='business_profiles';state.form=$('other-form');state.id=new URLSearchParams(location.search).get('id')||'';return true}return false}
function legacyIds(){return state.kind==='advisor'?{logo:'logo-file',photo:'photo-file'}:state.kind==='business'?{logo:'b-logo-file',photo:''}:{logo:'o-logo-file',photo:''}}
function normalizeLayout(v){return v==='distributed'||v==='grouped_left'||v==='stacked'?v:(v==='left'?'grouped_left':'stacked')}
function normalizeTextSize(v){return ['small','medium','large'].includes(v)?v:'medium'}
function normalizeTextAlign(v){return ['left','center','right'].includes(v)?v:'left'}
function normalizeCfg(c={}){return{...defaults,...c,logo_position:normalizeLayout(c.logo_position),header_text_size:normalizeTextSize(c.header_text_size),header_text_align:normalizeTextAlign(c.header_text_align)}}
function option(v,t){return`<option value="${v}">${t}</option>`}
function assetFieldset(){const ids=legacyIds();return $(ids.logo)?.closest('fieldset')||$(ids.photo)?.closest('fieldset')||null}
function assetPath(key){if(!state.profile)return'';return key==='main'?state.profile.logo_path:key==='icon'?state.profile.logo_icon_path:key==='dark'?state.profile.logo_dark_path:state.profile.photo_path}
function assetSource(key){const f=state.pending[key];if(f){if(!state.urls.has(key))state.urls.set(key,URL.createObjectURL(f));return state.urls.get(key)}if(key==='main'&&state.kind!=='advisor'&&!state.profile?.logo_path&&state.profile?.logo_data_url)return state.profile.logo_data_url;return storageUrl(assetPath(key))}
function revoke(key){const u=state.urls.get(key);if(u)URL.revokeObjectURL(u);state.urls.delete(key)}
function setPending(key,file){revoke(key);state.pending[key]=file||null;if(file)state.urls.set(key,URL.createObjectURL(file))}
function advisorHeaderControls(){
  if(state.kind!=='advisor')return'';
  return `<section class="vi-v4-config vi-v4-advisor-header-text"><div><strong>Texto del encabezado</strong><p class="hint">Este texto aparece junto al logo. Usa <strong>|</strong> para definir un salto de renglón exacto.</p></div><div id="vi4-advisor-label-slot"></div><div class="vi-v4-grid vi-v4-grid--2"><label>Tamaño del texto<select id="vi4-header-text-size">${option('small','Chico')}${option('medium','Mediano')}${option('large','Grande')}</select></label><label>Alineación<select id="vi4-header-text-align">${option('left','Izquierda')}${option('center','Centro')}${option('right','Derecha')}</select></label></div><p class="hint">En móvil el texto se centra automáticamente para conservar una composición equilibrada.</p></section>`;
}
function moveAdvisorLabelIntoPanel(){
  if(state.kind!=='advisor')return;
  const input=$('ally-label'),slot=$('vi4-advisor-label-slot');if(!input||!slot)return;
  const wrapper=input.closest('label');if(wrapper&&!slot.contains(wrapper)){wrapper.childNodes[0]&&(wrapper.childNodes[0].textContent='Texto junto al logo');slot.appendChild(wrapper)}
  const identityFieldset=input.closest('fieldset');
  document.querySelectorAll('#profile-form fieldset .hint').forEach(h=>{if(h.textContent?.includes('símbolo')&&h.textContent?.includes('salto de renglón'))h.remove()});
}
function inject(){const fs=assetFieldset();if(!fs||fs.querySelector('.vi-v4-panel'))return;fs.querySelectorAll('.asset-grid').forEach(el=>el.classList.add('vi-v4-legacy-hidden'));const details=fs.closest('.editor-accordion');if(details){details.querySelector('.accordion-title')&&(details.querySelector('.accordion-title').textContent='Identidad visual');details.querySelector('.accordion-subtitle')&&(details.querySelector('.accordion-subtitle').textContent='Logo, fotografía y composición del encabezado')}const p=document.createElement('div');p.className='vi-v4-panel';p.innerHTML=`<div class="vi-v4-assets"><article class="vi-v4-asset-card"><div class="vi-v4-card-head"><div><strong>Logotipo</strong><small>Imagen principal de la marca</small></div><label class="vi-v4-show"><input id="vi4-show-logo" type="checkbox"> Mostrar</label></div><div class="vi-v4-square"><img id="vi4-logo-img" alt="Logotipo"><span id="vi4-logo-empty">Sin imagen</span></div><div class="vi-v4-logo-choice"><label>Mostrar en encabezado<select id="vi4-logo-asset">${option('main','Logo principal')}${option('icon','Isotipo / icono')}</select></label></div><div class="vi-v4-actions"><button type="button" data-change="selected-logo">Cambiar</button><button type="button" data-delete="selected-logo">Eliminar</button></div><small id="vi4-logo-meta" class="vi-v4-meta"></small></article><article class="vi-v4-asset-card"><div class="vi-v4-card-head"><div><strong>Fotografía / perfil</strong><small>Imagen personal o del negocio</small></div><label class="vi-v4-show"><input id="vi4-show-photo" type="checkbox"> Mostrar</label></div><div class="vi-v4-square"><img id="vi4-photo-img" alt="Fotografía"><span id="vi4-photo-empty">Sin imagen</span></div><div class="vi-v4-actions"><button type="button" data-change="photo">Cambiar</button><button type="button" data-delete="photo">Eliminar</button></div><small id="vi4-photo-meta" class="vi-v4-meta"></small></article></div><input id="vi4-main-file" class="vi-v4-hidden-input" type="file" accept="image/png"><input id="vi4-icon-file" class="vi-v4-hidden-input" type="file" accept="image/png"><input id="vi4-dark-file" class="vi-v4-hidden-input" type="file" accept="image/png"><input id="vi4-photo-file" class="vi-v4-hidden-input" type="file" accept="image/jpeg,image/png,image/webp"><section class="vi-v4-config"><div><strong>Composición del encabezado</strong><p class="hint">En escritorio puedes distribuir, agrupar o apilar la identidad. En móvil la composición se adapta automáticamente.</p></div><div class="vi-v4-grid vi-v4-grid--2"><label>Preset<select id="vi4-preset">${option('personal','Personal')}${option('corporate','Corporativo')}${option('mixed','Mixto')}${option('minimal','Minimalista')}${option('custom','Personalizado')}</select></label><label>Composición<select id="vi4-layout">${option('distributed','Distribuido')}${option('grouped_left','Agrupado a la izquierda')}${option('stacked','Apilado centrado')}</select></label></div><div class="vi-v4-grid vi-v4-grid--3"><label>Tamaño del logo<select id="vi4-logo-size">${option('small','Chico')}${option('medium','Mediano')}${option('large','Grande')}</select></label><label>Forma del logo<select id="vi4-logo-shape">${option('none','Sin contenedor')}${option('circle','Circular')}${option('rounded','Cuadrado redondeado')}</select></label><label>Ajuste del logo<select id="vi4-logo-fit">${option('contain','Mostrar completo')}${option('cover','Rellenar contenedor')}</select></label><label>Tamaño de fotografía<select id="vi4-photo-size">${option('small','Chica')}${option('medium','Mediana')}${option('large','Grande')}</select></label><label>Forma de fotografía<select id="vi4-photo-shape">${option('circle','Circular')}${option('rounded','Cuadrada redondeada')}</select></label><label>Ajuste de fotografía<select id="vi4-photo-fit">${option('cover','Rellenar contenedor')}${option('contain','Mostrar completa')}</select></label></div></section>${advisorHeaderControls()}<section class="vi-v4-variants"><div class="vi-v4-variant"><div class="vi-v4-variant-thumb"><img id="vi4-light-img" alt="Logo modo claro"><span id="vi4-light-empty">Sin logo</span></div><div><strong>Logo modo claro</strong><small>Es el logo principal.</small><div class="vi-v4-mini-actions"><button type="button" data-change="main">Cambiar</button><button type="button" data-delete="main">Eliminar</button></div></div></div><div class="vi-v4-variant"><div class="vi-v4-variant-thumb vi-v4-variant-thumb--dark"><img id="vi4-dark-img" alt="Logo modo oscuro"><span id="vi4-dark-empty">Usará el principal</span></div><div><strong>Logo modo oscuro</strong><small>Opcional; sustituye al principal en fondo oscuro.</small><div class="vi-v4-mini-actions"><button type="button" data-change="dark">Cambiar</button><button type="button" data-delete="dark">Eliminar</button></div></div></div></section><p id="vi4-status" class="vi-v4-status" aria-live="polite"></p>`;fs.appendChild(p);moveAdvisorLabelIntoPanel();bind()}
function cfgNow(){const position=normalizeLayout($('vi4-layout')?.value);const out={preset:$('vi4-preset')?.value||'custom',show_logo:!!$('vi4-show-logo')?.checked,show_photo:!!$('vi4-show-photo')?.checked,logo_size:$('vi4-logo-size')?.value||'medium',photo_size:$('vi4-photo-size')?.value||'medium',logo_shape:$('vi4-logo-shape')?.value||'none',photo_shape:$('vi4-photo-shape')?.value||'circle',logo_fit:$('vi4-logo-fit')?.value||'contain',photo_fit:$('vi4-photo-fit')?.value||'cover',logo_position:position,logo_asset:$('vi4-logo-asset')?.value||'main'};if(state.kind==='advisor'){out.header_text_size=normalizeTextSize($('vi4-header-text-size')?.value);out.header_text_align=normalizeTextAlign($('vi4-header-text-align')?.value)}return out}
function setCfg(c){c=normalizeCfg(c);$('vi4-preset').value=c.preset||'custom';$('vi4-show-logo').checked=!!c.show_logo;$('vi4-show-photo').checked=!!c.show_photo;$('vi4-logo-size').value=c.logo_size;$('vi4-photo-size').value=c.photo_size;$('vi4-logo-shape').value=c.logo_shape;$('vi4-photo-shape').value=c.photo_shape;$('vi4-logo-fit').value=c.logo_fit;$('vi4-photo-fit').value=c.photo_fit;$('vi4-layout').value=normalizeLayout(c.logo_position);$('vi4-logo-asset').value=c.logo_asset||'main';if(state.kind==='advisor'){$('vi4-header-text-size')&&($('vi4-header-text-size').value=normalizeTextSize(c.header_text_size));$('vi4-header-text-align')&&($('vi4-header-text-align').value=normalizeTextAlign(c.header_text_align))}}
function status(msg='',kind=''){const el=$('vi4-status');if(!el)return;el.textContent=msg;el.dataset.kind=kind}
async function imageMeta(file){const bmp=await createImageBitmap(file),w=bmp.width,h=bmp.height,c=document.createElement('canvas');c.width=w;c.height=h;const x=c.getContext('2d',{willReadFrequently:true});x.drawImage(bmp,0,0);const d=x.getImageData(0,0,w,h).data;let alpha=false,minX=w,minY=h,maxX=-1,maxY=-1;for(let y=0;y<h;y++)for(let xx=0;xx<w;xx++){const a=d[(y*w+xx)*4+3];if(a<250)alpha=true;if(a>8){if(xx<minX)minX=xx;if(xx>maxX)maxX=xx;if(y<minY)minY=y;if(y>maxY)maxY=y}}return{w,h,alpha,minX,minY,maxX,maxY,bmp}}
async function prepareLogo(file){const m=await imageMeta(file);if(!m.alpha)throw new Error('El PNG seleccionado no contiene transparencia. Retira el fondo antes de cargarlo.');if(m.maxX<0)return file;let x0=m.minX,y0=m.minY,x1=m.maxX+1,y1=m.maxY+1,bw=x1-x0,bh=y1-y0,p=Math.max(4,Math.round(Math.max(bw,bh)*.02));x0=Math.max(0,x0-p);y0=Math.max(0,y0-p);x1=Math.min(m.w,x1+p);y1=Math.min(m.h,y1+p);bw=x1-x0;bh=y1-y0;if(bw>m.w*.94&&bh>m.h*.94)return file;const s=Math.min(1,1800/Math.max(bw,bh)),out=document.createElement('canvas');out.width=Math.max(1,Math.round(bw*s));out.height=Math.max(1,Math.round(bh*s));out.getContext('2d').drawImage(m.bmp,x0,y0,bw,bh,0,0,out.width,out.height);const blob=await new Promise(r=>out.toBlob(r,'image/png'));if(!blob)return file;status('PNG optimizado: se retiró margen transparente innecesario.','success');return new File([blob],file.name.replace(/\.png$/i,'')+'-optimizado.png',{type:'image/png'})}
async function validatePhoto(file){const bmp=await createImageBitmap(file);if(bmp.width<800||bmp.height<800)status(`Fotografía ${bmp.width}×${bmp.height}px · Resolución baja ⚠`,'warning');else status(`Fotografía ${bmp.width}×${bmp.height}px · Calidad adecuada ✓`,'success')}
function paint(imgId,emptyId,src){const img=$(imgId),empty=$(emptyId);if(src){img.src=src;img.hidden=false;empty.hidden=true}else{img.removeAttribute('src');img.hidden=true;empty.hidden=false}}
function loadMeta(src,el,key){if(!el)return;if(!src){el.textContent=key==='photo'?'Sin fotografía cargada':'Sin imagen cargada';return}const i=new Image();i.onload=()=>el.textContent=`${i.naturalWidth} × ${i.naturalHeight} px · ${key==='photo'?'Imagen':'PNG'}`;i.onerror=()=>el.textContent='Imagen cargada';i.src=src}
function render(){if(!state.profile)return;const selected=$('vi4-logo-asset')?.value||'main',logo=assetSource(selected),photo=assetSource('photo'),main=assetSource('main'),dark=assetSource('dark');paint('vi4-logo-img','vi4-logo-empty',logo);paint('vi4-photo-img','vi4-photo-empty',photo);paint('vi4-light-img','vi4-light-empty',main);paint('vi4-dark-img','vi4-dark-empty',dark);loadMeta(logo,$('vi4-logo-meta'),selected);loadMeta(photo,$('vi4-photo-meta'),'photo');document.querySelector('[data-delete="selected-logo"]')?.toggleAttribute('disabled',!logo);document.querySelector('[data-delete="photo"]')?.toggleAttribute('disabled',!photo)}
function markCustom(){if($('vi4-preset')&&$('vi4-preset').value!=='custom')$('vi4-preset').value='custom'}
function changeKey(key){if(key==='selected-logo')key=$('vi4-logo-asset').value==='icon'?'icon':'main';$(`vi4-${key}-file`)?.click()}
async function fileChanged(key,file){if(!file)return;try{if(key!=='photo'){if(file.type!=='image/png'&&!file.name.toLowerCase().endsWith('.png'))throw new Error('El logo debe ser PNG con fondo transparente.');file=await prepareLogo(file)}else await validatePhoto(file);setPending(key,file);render()}catch(e){status(e.message||'No se pudo leer la imagen.','error');const input=$(`vi4-${key}-file`);if(input)input.value=''}}
async function deleteAsset(key){if(key==='selected-logo')key=$('vi4-logo-asset').value==='icon'?'icon':'main';const path=assetPath(key);if(!path&&!state.pending[key])return;if(!confirm('¿Eliminar esta imagen?'))return;revoke(key);state.pending[key]=null;const col=key==='main'?'logo_path':key==='icon'?'logo_icon_path':key==='dark'?'logo_dark_path':'photo_path',payload={[col]:null,updated_at:new Date().toISOString()};if(key==='main'&&state.kind!=='advisor')payload.logo_data_url=null;const{data,error}=await supabase.from(state.table).update(payload).eq('id',state.id).select('*').single();if(error){status(error.message||'No se pudo eliminar la imagen.','error');return}if(path)await supabase.storage.from(cfg.storageBucket).remove([path]).catch(()=>{});state.profile=data;status('Imagen eliminada.','success');render()}
function bind(){['main','icon','dark','photo'].forEach(k=>$(`vi4-${k}-file`)?.addEventListener('change',e=>fileChanged(k,e.target.files?.[0])));document.querySelectorAll('[data-change]').forEach(b=>b.addEventListener('click',()=>changeKey(b.dataset.change)));document.querySelectorAll('[data-delete]').forEach(b=>b.addEventListener('click',()=>deleteAsset(b.dataset.delete)));$('vi4-logo-asset')?.addEventListener('change',()=>{markCustom();render()});['vi4-show-logo','vi4-show-photo','vi4-logo-size','vi4-photo-size','vi4-logo-shape','vi4-photo-shape','vi4-logo-fit','vi4-photo-fit','vi4-layout','vi4-header-text-size','vi4-header-text-align'].forEach(id=>$(id)?.addEventListener('change',markCustom));$('vi4-preset')?.addEventListener('change',()=>{const k=$('vi4-preset').value;if(presets[k]){setCfg({...cfgNow(),...presets[k],preset:k});render()}});state.form.addEventListener('submit',()=>{state.saving=saveAll()},{capture:true})}
async function upload(key,file){const ext=key==='photo'?(file.name.split('.').pop()||'jpg').toLowerCase():'png',owner=state.profile?.user_id||'shared',path=`${owner}/${state.id}/vi5-${key}-${Date.now()}.${ext}`;const{error}=await supabase.storage.from(cfg.storageBucket).upload(path,file,{cacheControl:'3600',upsert:false,contentType:file.type});if(error)throw error;return path}
async function saveAll(){if(!state.profile||!state.id)return;status('Guardando identidad visual…','');try{const visual={...(state.profile.visual_identity||{}),...cfgNow()};visual.logo_position=normalizeLayout(visual.logo_position);const payload={visual_identity:visual,updated_at:new Date().toISOString()},old={};for(const key of ['main','icon','dark','photo']){const file=state.pending[key];if(!file)continue;const col=key==='main'?'logo_path':key==='icon'?'logo_icon_path':key==='dark'?'logo_dark_path':'photo_path';old[key]=assetPath(key);payload[col]=await upload(key,file)}const{data,error}=await supabase.from(state.table).update(payload).eq('id',state.id).select('*').single();if(error)throw error;state.profile=data;for(const key of Object.keys(old)){if(old[key])await supabase.storage.from(cfg.storageBucket).remove([old[key]]).catch(()=>{});setPending(key,null);const input=$(`vi4-${key}-file`);if(input)input.value=''}status('Identidad visual guardada.','success');render();window.dispatchEvent(new CustomEvent('visual-identity:saved',{detail:{id:state.id}}))}catch(e){status(e.message||'No fue posible guardar la identidad visual.','error');window.dispatchEvent(new CustomEvent('visual-identity:error'))}}
async function loadProfile(){if(state.kind==='advisor'){const slug=$('slug')?.value.trim();if(!slug)return;const{data}=await supabase.from(state.table).select('*').eq('slug',slug).maybeSingle();state.profile=data;state.id=data?.id||'';state.lastSlug=slug}else{if(!state.id)return;const{data}=await supabase.from(state.table).select('*').eq('id',state.id).maybeSingle();state.profile=data}if(state.profile){setCfg(normalizeCfg(state.profile.visual_identity||{}));render()}}
function enforceAccordions(){document.querySelectorAll('details.editor-accordion').forEach(d=>{if(d.dataset.vi5Accordion)return;d.dataset.vi5Accordion='1';d.addEventListener('toggle',()=>{if(d.open){document.querySelectorAll('details.editor-accordion').forEach(o=>{if(o!==d)o.open=false});document.querySelectorAll('details.editor-accordion').forEach(o=>o.classList.toggle('is-active-section',o===d))}else d.classList.remove('is-active-section')})});const open=[...document.querySelectorAll('details.editor-accordion[open]')];open.slice(1).forEach(d=>d.open=false)}
async function init(){if(!detect())return;inject();enforceAccordions();new MutationObserver(enforceAccordions).observe(state.form,{childList:true,subtree:true});if(state.kind!=='advisor')await loadProfile();else{for(let i=0;i<40&&!$('slug')?.value;i++)await new Promise(r=>setTimeout(r,150));await loadProfile();setInterval(async()=>{const s=$('slug')?.value.trim();if(s&&s!==state.lastSlug){Object.keys(state.pending).forEach(k=>{revoke(k);state.pending[k]=null});await loadProfile()}},600)}}
window.addEventListener('beforeunload',()=>state.urls.forEach(u=>URL.revokeObjectURL(u)));init().catch(console.error);
