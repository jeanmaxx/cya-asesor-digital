import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cfg=window.SUPABASE_CONFIG||{},supabase=createClient(cfg.url,cfg.publishableKey),qs=new URLSearchParams(location.search);
const storageUrl=p=>p?`${cfg.url}/storage/v1/object/public/${cfg.storageBucket}/${p}`:'';
function choose(p,c){if(c.logo_asset==='icon'&&p.logo_icon_path)return storageUrl(p.logo_icon_path);if(document.documentElement.dataset.theme==='dark'&&p.logo_dark_path)return storageUrl(p.logo_dark_path);return storageUrl(p.logo_path)||(p.logo_data_url||'')}
async function init(){if(!location.pathname.includes('/otros/'))return;const slug=qs.get('negocio')||'tu-tarjeta-digital';const{data:p}=await supabase.from('business_profiles').select('*').eq('slug',slug).eq('vertical','other').maybeSingle();if(!p)return;const c={logo_position:'above',logo_asset:'main',show_logo:true,show_photo:false,logo_fit:'contain',...(p.visual_identity||{})};
 const apply=()=>{const row=document.querySelector('.brand-row'),mark=document.getElementById('brand-mark');if(!row||!mark)return;row.classList.remove('vi-public-center','vi-public-side');row.classList.add(c.logo_position==='left'?'vi-public-side':'vi-public-center');document.body.style.setProperty('--vi-logo-fit',c.logo_fit||'contain');const src=choose(p,c);let img=mark.querySelector('img');if(src){if(!img){mark.textContent='';img=document.createElement('img');img.alt=`Logo de ${p.business_name||'negocio'}`;mark.appendChild(img)}if(img.src!==src)img.src=src;img.removeAttribute('style')}mark.classList.toggle('vi-hidden',!c.show_logo);};
 for(let i=0;i<50&&!document.getElementById('hero-title')?.textContent;i++)await new Promise(r=>setTimeout(r,100));apply();new MutationObserver(apply).observe(document.documentElement,{attributes:true,attributeFilter:['data-theme']});}
init().catch(()=>{});
