import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cfg=window.SUPABASE_CONFIG||{},supabase=createClient(cfg.url,cfg.publishableKey),$=id=>document.getElementById(id);
const storageUrl=p=>p?`${cfg.url}/storage/v1/object/public/${cfg.storageBucket}/${p}`:'';
function context(){if($('other-form'))return{table:'business_profiles',id:new URLSearchParams(location.search).get('id')||''};if($('business-form'))return{table:'business_profiles',id:new URLSearchParams(location.search).get('id')||''};if($('profile-form'))return{table:'advisor_profiles',slug:$('slug')?.value?.trim()||''};return null}
async function digest(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error('fetch');const b=await r.arrayBuffer();const h=await crypto.subtle.digest('SHA-256',b);return [...new Uint8Array(h)].map(x=>x.toString(16).padStart(2,'0')).join('')}
function warningNode(){let n=$('vi4-variant-warning');if(n)return n;const variants=document.querySelector('.vi-v4-variants');if(!variants)return null;n=document.createElement('p');n.id='vi4-variant-warning';n.className='vi-v4-status';variants.insertAdjacentElement('afterend',n);return n}
async function check(){const c=context();if(!c)return;for(let i=0;i<60&&!document.querySelector('.vi-v4-variants');i++)await new Promise(r=>setTimeout(r,100));const n=warningNode();if(!n)return;let q=supabase.from(c.table).select('logo_path,logo_dark_path');q=c.id?q.eq('id',c.id):q.eq('slug',c.slug);const{data}=await q.maybeSingle();if(!data?.logo_path||!data?.logo_dark_path){n.textContent='';n.dataset.kind='';return}try{const[a,b]=await Promise.all([digest(storageUrl(data.logo_path)),digest(storageUrl(data.logo_dark_path))]);if(a===b){n.textContent='⚠ El logo de modo claro y el logo de modo oscuro son idénticos. Carga una variante diferente si necesitas contraste distinto entre ambos temas.';n.dataset.kind='warning'}else{n.textContent='Variantes claro y oscuro diferentes ✓';n.dataset.kind='success'}}catch{n.textContent=''} }
check().catch(()=>{});
