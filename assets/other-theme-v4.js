import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cfg=window.SUPABASE_CONFIG||{},supabase=createClient(cfg.url,cfg.publishableKey),qs=new URLSearchParams(location.search),slug=qs.get('negocio')||'tu-tarjeta-digital';
let profile=null;
function setTheme(t,persist=true){t=t==='dark'?'dark':'light';const root=document.documentElement;root.dataset.theme=t;const s=root.style;s.setProperty('--brand',profile?.primary_color||'#0B1F36');s.setProperty('--accent',profile?.accent_color||'#27C2C7');if(t==='dark'){s.setProperty('--bg','#07111D');s.setProperty('--surface','#0E1A28');s.setProperty('--text','#F3F7FB');s.setProperty('--muted','#9BACBE');s.setProperty('--line','#203347')}else{s.setProperty('--bg',profile?.background_color||'#F5F8FB');s.setProperty('--surface',profile?.surface_color||'#FFFFFF');s.setProperty('--text','#132033');s.setProperty('--muted','#667386');s.setProperty('--line','#DCE4EC')}const btn=document.getElementById('theme-toggle'),icon=document.getElementById('theme-icon');if(icon)icon.textContent=t==='dark'?'☀':'☾';if(btn){const label=t==='dark'?'Cambiar a modo claro':'Cambiar a modo oscuro';btn.setAttribute('aria-label',label);btn.title=label}if(persist)localStorage.setItem(`other-theme:${slug}`,t)}
async function init(){if(!location.pathname.includes('/otros/'))return;const{data}=await supabase.from('business_profiles').select('primary_color,accent_color,background_color,surface_color,theme_mode').eq('slug',slug).eq('vertical','other').maybeSingle();profile=data||{};for(let i=0;i<60&&!document.getElementById('theme-toggle');i++)await new Promise(r=>setTimeout(r,100));const btn=document.getElementById('theme-toggle');if(!btn)return;const initial=localStorage.getItem(`other-theme:${slug}`)||(profile.theme_mode==='system'?(matchMedia('(prefers-color-scheme:dark)').matches?'dark':'light'):(profile.theme_mode||'light'));setTheme(initial,false);btn.onclick=null;btn.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();setTheme(document.documentElement.dataset.theme==='dark'?'light':'dark',true)},true)}
init().catch(console.error);
